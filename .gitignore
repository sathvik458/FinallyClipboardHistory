# Binaries built locally
/bin/
glassclipd

# macOS noise
.DS_Store

# Xcode / Swift build artifacts
ui/GlassClip/.build/
ui/GlassClip/*.xcodeproj/xcuserdata/
DerivedData/

# Private notes — never commit
INTERVIEW_PREP.md
